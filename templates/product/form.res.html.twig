<div class="container my-5">
    <div class="row">
        <div class="col">
            {{form_start(form)}}

            <button type="submit" class="btn btn-success">Je le prends!</button>
            
            {{form_end(form)}}
        </div>
    </div>
</div>